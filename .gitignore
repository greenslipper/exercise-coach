.env
.venv/
__pycache__/
*.pyc
.DS_Store

# Strava data — personal info, never commit
data/activities/
data/athlete.json
data/sync_log.json

# Google Calendar credentials — never commit
data/calendar_credentials.json
data/calendar_token.json
data/calendar.json

# Worker sync cache — personal data, never commit
data/gym_log.json
data/weight_log.json
